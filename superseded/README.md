# superseded/ — archived legacy methodology (NOT canonical)

These trees are the OLD North Star line NBCLI was originally forked from:

- `north-star-blueprint-v6.0/` — North Star Blueprint **v6.0** (legacy "HOW").
- `master-build-framework-v2.0/` — MBF **v2.0** (legacy "WHAT").

They are retained for provenance only and are **not** the source of truth.

**Canonical methodology = NBB v6.5 / MBF v2.5** (`Navigata1/NBB`, pinned in
`docs/ADR-001-nbb-realignment.md`). The doctrine NBCLI WILL enforce/emit (Slices 2-3) is vendored + verified under
`vendor/nbb/` (`nsb sync --check`). The full Blueprint v6.5 / MBF v2.5 are referenced on demand from
NBB (tokenomics: never front-load).

License: this archived content is CC BY-NC-SA 4.0 (c) North Star Build / @NavigatingTruth (see root `NOTICE`).
